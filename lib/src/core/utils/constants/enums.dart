enum Status { initial, loading, empty, success, error }
enum SubmissionStatus { initial, submitting, success, error }
enum OperationMode { render, selection, filtration }
enum ActionMode { create, edit, duplicate }